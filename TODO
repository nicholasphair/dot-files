+ Update setup.py to set up our .history directory.
+ Write a function to change cd to cd;ls. and come up with a good name.
+ Set up local bash completions.
+ Add functionality to automatically grab the needed packages. Might make sense
to ditch python and move to a bash script.
+ Increase portability.
+ Fix screen color and statusline issues.
+ Automatically call pluginstall.
+ Update readmes.

---
This will automatically call pluginstall and quit.
vim +silent +PlugInstall +qall
