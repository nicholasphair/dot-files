# Note to future self: screen on the mac does not come with 256 colors.
# You'll have to build it from source.

# Enable background color erase. Required for centOS.
defbce on

# Restore display when exiting new window like apps such as vim.
# TODO: Doesn't work like it once did...
altscreen on

# Don't show startup message and cache 30000 lines for scroll back
startup_message off
defscrollback 30000

# Enables mouse scroll somehow.
termcapinfo xterm* ti@:te@

# TODO: Get a better statusline. One with a clock.
hardstatus string '%{= kw}%-w%{+b yk}[%n %t%?(%u)%?]%{-}%+w %=%{g}%{= kG}[%{G}%H%? %1`%?%{g}]'
hardstatus alwayslastline 

# Show hardstatus: ctrl-a F. Hide hardstatus: ctrl-a f.
bind f eval "hardstatus ignore"
bind F eval "hardstatus alwayslastline"

# Turn of the annoying screen bell.
vbell off
bell_msg ""
